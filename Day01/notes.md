#day 1 notes 
#today i learned from the official postgresql documentation 
Querying Data
SELECT 
Column Aliases 
ORDER BY 
SELECT DISTINCT 
DISTINCT ON

#important points 
1. postgresql is free to return rows in any order if you don't use an order by clause, so always use an order by clause 
2. concatenation operator is || , we used it for composite attributes
3. to use aliases, use keyword as 
